<%- | $nets_self6,
| -%>
# DO NOT EDIT!
# This file is puppet generated.

protocol bfd 'backbone-bfd' {
  interface "bb-*" {
    interval 200 ms;
    multiplier 10;
  };
}

protocol ospf 'backbone-ospf' {
  import filter {
    if !(net ~ [ <%= join($nets_self6, ",\n      ") %> ]) then preference = 50;
    accept;
  };
  export where net ~ [ ::/0,
    <%= join($nets_self6, ",\n    ") %> ];
  area 0.0.0.0 {
    interface "bb-*" {
      bfd;
    };
  };
}

